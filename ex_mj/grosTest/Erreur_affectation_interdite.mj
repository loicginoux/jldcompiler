import PointCol;
import Point;

class Erreur_affectation_interdite {

		public static int fact(int n) {
				if(n <1) { 
			    		return 1; 
			    } else {
			    		return n * fact(n-1);
			    }
		}
		
		public static void main() {
				PointCol pc = new PointCol(2, 4, 24);
				syso(fact(pc.gety()));
				pc.milieu(null) = new Point(2, 4);
				int x;
				if (x = 3) {}
		}

}