import PointCol;
import PointCol;

class Erreur_duplicate_import {

		public static int fact(int n) {
				if(n <1) { 
			    		return 1; 
			    } else {
			    		return n * fact(n-1);
			    }
		}
		
		public static void main() {
				PointCol pc = new PointCol(2, 4, 24);
				syso(fact(pc.gety()));
		}

}