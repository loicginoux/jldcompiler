import PointCol;

class Erreur_main_non_statique {

		public static int fact(int n) {
				if(n <1) { 
			    		return 1; 
			    } else {
			    		return n * fact(n-1);
			    }
		}
		
		public void main() {
				PointCol pc = new PointCol(2, 4, 24);
				syso(fact(pc.gety()));
		}

}